# CLAUDE.md

KusumaVision NMS — FTTH/GPON Network Management System (Laravel 12 + Vue 3/Inertia + Tailwind) owned by PT BERKAH MEDIA KUSUMA VISION (BMKV). A SmartOLT/NetNumen alternative for Indonesian ISPs to manage ZTE GPON OLTs and provision ONUs.

The user communicates in Indonesian — respond in Indonesian (bilingual when discussing code/English terms).

**Detailed developer docs live in `docs/handbook/`** (per-topic: overview, architecture, folder map, install/deploy, DB schema, routing, modules, SNMP/polling, CLI/telnet, alarm/Telegram, security/RBAC, frontend, troubleshooting, how-to-add-features). Start there for deep dives, and keep it in sync when structure or conventions change. License is MIT (`LICENSE`).

## Commands

```bash
composer dev        # serve + queue + pail logs + vite (concurrently)
php artisan serve --host=0.0.0.0 --port=8000
npm run dev         # vite
bash scripts/test.sh # PHPUnit — SELALU lewat skrip ini, JANGAN `php artisan test` polos (lihat Conventions)
npm test            # Vitest (tests komponen Vue di tests/js/*.spec.js; config vitest.config.js)
./vendor/bin/pint   # code style
php artisan telnet:proxy           # WebSocket<->telnet proxy daemon (browser terminal)
go build -o bin/kv-snmp-poller ./cmd/kv-snmp-poller   # rebuild Go SNMP poller
sudo bash install.sh               # one-command deploy on a fresh Ubuntu server (--yes for non-interactive)
bash scripts/check-requirements.sh # verify tools/extensions/artifacts/services (+ min versions)
docker compose up -d --build       # "appliance" deploy: whole stack in containers on any PC (start.bat/start.sh 1-click); see docs/DOCKER.md
```

App DB is PostgreSQL (`kusumavision_nms`), Redis for cache/session/queue, locale `id`. Tests run on in-memory sqlite, so migrations must stay sqlite-compatible.

## Scope reality (important)

**We don't use the C-Data, HiOSO, or HsAirPo modules** — avoid touching `CDataOltController`/`HiosoOltController`/`HsAirPoOltController`, their services, migrations, or `Pages/{CDataOlt,Hioso,HsAirPo}/*.vue` unless a change there is strictly necessary to complete the actual task at hand (e.g. a shared service both families depend on). Don't proactively extend, refactor, or add features to these modules on your own initiative.

The PRD (`KusumaVision_NMS_PRD.md`) describes a broad vision; the **built scope is narrower**. Do not assume PRD features exist:

- **ZTE C300/C320/C600 supported** (plus non-ZTE C-Data EPON/GPON, HiOSO EPON & HsAirPo/HSGQ EPON, see Architecture). `App\Support\SmartOltSupport::driverKey()` returns `unknown` for anything else and disables all capabilities. **C600** (`SmartOltSupport::isC600()`, dikenali dari sysDescr **atau** sysObjectID `3902.1082.1001.600`) memakai subtree `.1082` (zxAccessNode) — tapi **cakupannya lebih sempit dari C300/C320**: tabel ONU di `.1082.500.20.2.1.2.1.*` (SN `.3`, model `.8`; phase-state kini dari `.1082.500.10.2.3.8.1.4` = 2 LOS/4 Working/5 DyingGasp/7 OffLine — menggantikan `.7` biner, set online identik 1343/1343; admin-state `.10.2.3.8.1.1` = 1 enable/2 disable; nama `.10.2.3.3.1.2`) dan **Rx ONU di `.1082.500.20.2.2.2.1.10`** (index `{ifIndex}.{onuId}.{onuPort}`, kembaran OID C300 `.1012.3.50.12.1.1.10` → berbagi parsing & skala; sentinel `65535`). Rx OLT-side (upstream, metrik **beda**) ada di `.1082.500.1.2.4.2.1.2` (milli-dBm, sentinel `-80000`) — belum dipakai. **Nama ONU, admin-state, phase kaya & unconfigured-discovery KINI terpetakan via SNMP** (Jul 2026, terverifikasi live): nama/deskripsi di `.1082.500.10.2.3.3.1.2/.3` — SNMP mengembalikan nama pelanggan asli WALAU CLI `show gpon onu detail-info` menyensor `Name`/`Description` jadi `********`; unconfigured di `.1082.500.2.2.11.2.1.2` (serial). Docs: `docs/ZTE_C600_Configured_ONU_Name_SNMP_Discovery.md`, `docs/ZTE_C600_Unconfigured_ONU_SNMP_Discovery.md`. Yang masih OFF: tulis (`supports_onu_toggle`/`supports_onu_info_write` = false untuk C600). **Penamaan CLI C600 = 3-tier `gpon_olt-1/{slot}/{port}` / `gpon_onu-1/{slot}/{port}:{id}` / `xgei-1/{slot}/{port}`** (terbukti dari running-config; asumsi 4-tier `gpon-olt_1/1/…` yang lama SALAH). **Provisioning C600 KINI ON** (`supports_provisioning=true`, Jul 2026): mode **Model B / SmartOLT TR069** via `ZteC600ProvisioningScriptBuilder` — struktur direproduksi persis dari running-config ONU asli (vport, dua service internet+mgmt, mgmt-ip in-band, VEIP/ACS, tr069-mgmt `tag pri`), `end` sebelum `write` (write invalid di config mode); pendukung: alokasi mgmt-IP otomatis (scan pool OLT, un-wrap IP terpotong line-wrap), dropdown ONU Type/T-CONT dari katalog, preset ACS dari OLT; WAN pppoe/dhcp/static **tetap ditolak** di C600. Yang masih OFF di C600: reconfigure/edit ONU (`supports_onu_config_write=false`). Kartu C600 nyata: GFGL/GFGM/GFGN (GPON 16-port), SFUB (uplink xgei). Semua terverifikasi live Jul 2026 — OID **tabel-ONU** lama (`.1082.500.10.2.3/.2.8/.2.11` sebagai tabel ONU) salah total & bikin C600 terbaca 0 ONU; tapi sub-tabel lain di subtree yang sama TERBUKTI benar & terpisah: `.10.2.3.3.1` (nama/deskripsi) dan `.10.2.3.8.1` (admin/phase). Referensi: `docs/SMARTOLT_ZTE_C600_GUIDE.md`.
- **SNMP read = v1/v2c only.** v3 throws (`OltSnmpClient`).
- **CLI execution = Telnet only.** `ZteCliProvisioningExecutor` rejects SSH (phpseclib installed but no SSH driver wired). Interactive browser telnet also Telnet-only (see Architecture).
- **No TimescaleDB, no `onus` table (per-OLT ONU state lives in the `last_test_result` JSON), no multi-vendor beyond the above, no AI** — these remain PRD vision, not code. RX history IS persisted though (`onu_rx_samples` table, sampled by `PollOltJob`, charted by `RxTrendCard`/`PortDetail`/`OnuDetail`).
- **Scheduled polling DOES use a Go engine** (`bin/kv-snmp-poller`, source `cmd/kv-snmp-poller/`) — see Architecture. On-demand actions (manual refresh, provisioning, telnet, per-port RX) are synchronous PHP in controllers/services — **except batch jobs**: copy-ONU-to-port (`app/Jobs/CopyOnusToPortJob.php` + `copy_onu_tasks`) and **bulk-enable-TR069** (`app/Jobs/Tr069BulkConfigJob.php` + `tr069_bulk_tasks`), both polled for progress.
- Per-OLT live state is cached as JSON in `snmp_olts.last_test_result`.

## Architecture

- `app/Models/SnmpOlt.php` — OLT inventory. Secrets (`snmp_read_community`, `snmp_write_community`, `cli_password`) use `encrypted` casts and are `$hidden`. On edit, empty secret fields preserve existing values (`withoutEmptySecrets`).
- `app/Services/Snmp/OltSnmpClient.php` — SNMP read: system info, GPON ports (IF-MIB), ZTE ONU table, unconfigured ONU discovery. Parsing quirks are tuned against real firmware (`OLT-C320-PATI`).
- `app/Services/ZteCliProvisioningExecutor.php` — Telnet session; auto-continues `--More--` pagers; masks CLI password in captured output.
- `app/Services/ZteProvisioningScriptBuilder.php` — builds ONU provisioning CLI scripts (register, VLAN/T-CONT, PPPoE/DHCP/static WAN, TR069, Remote ONT).
- `app/Services/ZteProfileCatalogService.php` — per-OLT profile sync; runs `show ...` commands and parses onu_type/tcont/vlan/ip profiles.
- `app/Services/ZteOnuRxPowerService.php` — ONU RX power via `show pon power onu-rx`.
- `app/Http/Controllers/SmartOltController.php` + `SmartOltProfileController.php` — all SmartOLT routes (`routes/web.php`, auth-protected). `onuMonitor()`/`refreshOnuMonitor()` back the cross-OLT **ONU Monitoring** page (route `monitoring.onu`, `Pages/SmartOlt/OnuMonitor.vue`): aggregates cached `port_onus` across all OLTs; refresh does one full-OLT SNMP walk and writes per-port cache. Per-port page (`Pages/SmartOlt/PortOnus.vue`) also has: **copy ONU config to another port** (batch — reads each source ONU's running-config in one telnet session, rebuilds a full registration via `ZteOnuReconfigureScriptBuilder::buildForCopy()`, queued job + progress modal), **delete ONU** (`no onu {id}`, gated `supports_onu_delete`). Per-port refresh (`portOnusSnapshot`) walks only the port's ONU-table subtree on C300/C320 (scoped by `zteEncodeIfIndex` prefix) — not the whole OLT.
- **Go SNMP poller** — `cmd/kv-snmp-poller/main.go` (+ `go.mod`) builds to `bin/kv-snmp-poller`. `app/Services/Snmp/GoSnmpPoller.php` shells out to it (Symfony Process, JSON out); `enabled()` when `SNMP_POLLER_DRIVER=go` (`config/services.php`) and the binary exists. `app/Jobs/PollOltJob.php` (scheduled, runs under supervisor worker) uses the Go poller when enabled and **falls back to `OltSnmpClient`** on failure. Prod `.env` sets `SNMP_POLLER_DRIVER=go`.
- **Non-ZTE (C-Data & HiOSO) ikut scheduled polling** — `PollOltJob` mendeteksi family non-ZTE (`SmartOltSupport::isNonZte` = C-Data EPON/GPON **atau** HiOSO) lalu mengambil jalur scanner `pollViaScanner()`: scan penuh via `CDataOltScanner` (driver EPON SNMP / GPON V3 SNMP+CLI menulis `last_test_result.port_onus` bentuk-ZTE), set penanda `ok` top-level (dibutuhkan `AlarmEvaluator`, scanner tak menyetelnya), catat sampel RX (`onu_rx_samples`) saat RX due, evaluasi alarm, log `PollingEvent`. `PollOltsCommand` (`olts:poll`, tiap menit) men-dispatch SEMUA OLT `polling_enabled` yang due (ZTE **dan** non-ZTE). On-demand refresh C-Data (`CDataOltController`) tetap ada untuk refresh manual/halaman. **Aksi write ONU C-Data** (EPON & GPON, via `CDataCliWriteService` — telnet `interface {epon|gpon} 0/{slot}`): rename `ont description`, reboot `ont reboot`, **enable/disable** (gated `supports_onu_toggle`, route `cdata-olt.onu.state`: EPON `ont enable|disable {port} {onuId}`, GPON `ont activate|deactivate {port} {onuId}` — **beda verb per-family**), **delete `ont delete {port} {onuId}`** (gated `supports_onu_delete`, route `cdata-olt.onu.delete`) — sintaks delete/rename/reboot identik EPON/GPON, terverifikasi help CLI live. **Remote ONT (GPON FlashV3 saja)**: buka/tutup akses web ONT dari WAN via `ont security-mgmt {port} {onuId} 1 state enable mode forward protocol web` (klon sintaks ZTE, TAK ada di manual resmi — ketemu via context-help live; push OMCI efek instan, dipatuhi juga ONT merk ZTE/F660) — `CDataCliWriteService::setRemoteAccess`, route `cdata-olt.onu.remote-access`, gated `supports_onu_remote_access` (V3 only); audit rule via `show current-config` (BUKAN `show running-config`). **Faceplate panel-depan**: `CDataFaceplateService` membaca IF-MIB (klasifikasi port PON/GE/XGE) + tabel device `17409.2.3.1.*` (model/serial/versi), di-cache `last_test_result.panel` oleh scanner, divisualkan `Components/CDataOlt/OltFaceplate.vue` di halaman Detail. Referensi OID/CLI lengkap: `docs/SMARTOLT_CDATA_GUIDE.md`.
- **HiOSO / V-Sol EPON (enterprise 25355, mis. HA7304)** — family non-ZTE ke-4. `SmartOltSupport::driverKey()` mengenalinya (`DRIVER_HIOSO_EPON`; needle `hioso|ha7304|25355|v-sol|vsol`) → `capabilities()` snmp_rx + rename/reboot/delete/enable-disable on (provisioning off). Driver `app/Services/Hioso/HiosoEponSnmpService.php` (implements `SmartOltSnmpDriver`, di-resolve `SmartOltSnmpServiceResolver`) walk **4 OID kanonik** index `.{PON}.{ONU}` (slot selalu 1): nama `25355.3.2.6.3.2.1.37.1`, MAC `25355.3.2.6.3.2.1.11.1`, Rx `25355.3.2.6.14.2.1.8.1` (string dBm), **link-state `25355.3.2.6.3.2.1.39.1` (1=Up, 2=Down)**. **Status online WAJIB dari link-state, JANGAN disimpulkan dari Rx** (terverifikasi live Agu 2026 lintas OLT & varian, cocok 1:1 dgn CLI `show onu info epon 0/{PON} all`): sebagian ONU tak dilaporkan DDM-nya oleh OLT → Rx `na` permanen padahal link Up & trafik jalan, dan logika lama (Rx=status) membuat pelanggan aktif tampil offline + alarm palsu (gejala WIDOROKANDANG PON 1: 10 ONU Up, hanya 2 punya Rx; PEKALONGAN PON 3). Rx valid tetap dipakai sebagai bukti pendukung online, tak pernah sebaliknya; nilai Rx lama dibawa `snmp_stale` maks `MAX_RX_NA_STRIKES` (2) poll lalu dikosongkan agar tak menampilkan angka redaman beku (`rx_na_strikes` per-ONU). Walk RX/Nama/link-state pakai `robustWalk` dgn **target-kelengkapan dari tabel MAC** supaya polling terjadwal di link lossy tak salah tandai ONU sebagai offline. **Anti-flap**: ONU yang tadinya online baru ditandai offline di snapshot setelah TERAMATI down beruntun `MAX_OFFLINE_STRIKES` (2) poll (`offline_strikes` per-ONU), supaya 1 pembacaan buruk tak bikin dashboard/faceplate (status port turunan jumlah ONU online) "berkedip" down/up — murni penghalus tampilan; gerbang alarm ada di debounce 2-poll `AlarmEvaluator`. Baris link-state & Rx yang sama-sama ABSEN dari walk = walk terpotong → pertahankan status terakhir. **Jangan walk** `25355.3.2.6.2.1.*` (puluhan ribu entry). **Punya controller + rute + halaman sendiri** (dipisah dari C-Data): `HiosoOltController` + rute `hioso-olt.*` + `Pages/Hioso/*` (Create/Edit/Detail/PortOnus + `Partials/HiosoOltForm`; reuse komponen presentasi `Components/CDataOlt/OltFaceplate`). Tab "OLT HiOSO" di halaman SmartOLT (`SmartOltController::index()` partisi 3-arah zte/cdata/hioso; tab cdata & hioso berbagi body tabel non-ZTE, prefix rute dipilih `isHiosoTab` di `Index.vue`). Aksi tulis ONU via `HiosoCliWriteService` (CLI telnet `conf t` → `interface epon 0/{port}` → `onu {id} name|reboot`, **enable/disable** = `onu {id} activate|deactivate` [terverifikasi live via context-help, gated `supports_onu_toggle`, rute `hioso-olt.onu.state`], delete = `delete onu {id}` [terverifikasi live; **bukan** `no onu {id}` / `onu {id} delete` yang ditolak], gated `supports_onu_delete`); scan penuh tetap lewat `CDataOltScanner` bersama. **Pemilihan rute lintas halaman** (global search, ONU monitoring, peta) via `SmartOltSupport::inventoryRoutePrefix($driver)` → `smartolt`/`cdata-olt`/`hioso-olt`; `OnuInventoryService` & pin peta (`OnuMapController`) menyertakan field `port_route`, reboot/rename pin peta punya cabang HiOSO. **Varian HA7302** (HA7302CSM/CST 2-port, mis. HA7302CSM v7.76 live — `SmartOltSupport::isHiosoHa7302()`, deteksi firmware/vendor/name memuat `ha7302`): SNMP **beda** dari HA7304 — ONU = ruang **LLID datar** `1..128` (index `.{oltId=1}.{onu}`, TAK ada `Pon-Nni` → `getPorts()` fallback **1 port EPON agregat**; scoping walk pakai `ponNumbers()` mentah, kosong=full-table fallback — OID nama/MAC/Rx kanonik tetap sama, inventory+Rx terbaca), telnet **3-lapis password** (login+Access+Enable, semua `cli_password`) + **IAC telnet WAJIB** (agen menahan banner login sampai opsi dijawab → `HiosoCliWriteService::readUntil` IAC-aware via `TelnetIacFilter` khusus HA7302; HA7304 tak berubah) & dialek CLI beda (`pon 1/{port}`→`set onu {id} reboot`, `delete onu 1/{port}/{onu}`, enable/disable `set pon 1/{port} onu {id} auth-mode pass|deny`; tanpa `interface epon`; perintah `show pon`/`show onu all` BUG → listing pakai `search mac-address {mac} mask 1`). Pemetaan LLID-datar→CLI **terverifikasi 1:1** (CLI onuId == index flat SNMP, PON=1; 1 PON = 128 ONU, bukan split /64) → **aksi CLI per-ONU AKTIF** (`supports_reboot/onu_toggle/onu_delete/config_save=true`); **rename via SNMP SET** (`description_mode='snmp'` → `HiosoEponSnmpService::setOnuName()`/`HiosoSnmp::set()` ke OID nama writable `.37.1.{oltId}.{onu}`, karena CLI HA7302 tak punya rename), bercabang di `HiosoOltController`/`Api/V1/OnuActionController`/`OnuMapController`. HA7304 tak berubah. Referensi: `docs/SMARTOLT_HIOSO_GUIDE.md` (§1–§11 vendor; §12 dari project lain, abaikan).
- **HsAirPo / HSGQ EPON (OEM Photon Broadband, enterprise 12170, "4PON EPON-OLT")** — family non-ZTE ke-5, **Fase A read-only** (Jul 2026). Satu-satunya family **CLI-first**: SNMP perangkat ini **tak punya tabel ONU sama sekali** (terverifikasi full-walk; `storage/app/photon_12170_snmp_fullwalk.txt`), jadi inventori ONU dibaca CLI `show epon onu all info` (1 perintah/scan, kolom PON/ONU-ID/MAC/Run-state → record bentuk-ZTE `slot=1`, `port`=PON, interface `pon{n}:{id}`), sedang SNMP hanya untuk MIB-2 sistem + port PON `pon1..pon4` (ifIndex 5002-5005) + skalar vendor (nama manufaktur `.12170.2.3.1.1.12.0`, jumlah ONU online per-PON `.12170.2.3.3.1.1.8.1.0.{pon}` — cocok 1:1 dgn CLI). ⚠️ **sysDescr KOSONG** → deteksi andal lewat sysObjectID `.1.3.6.1.4.1.12170.2.3` (needle `12170|hsairpo|hsgq|photon`, diperiksa **sebelum** needle `epon` C-Data). ⚠️ **Jangan walk subtree enterprise 12170** (cabang `.2.3.1.2` & `.2.3.2.4` GETNEXT-nya loop tak berhingga) — ambil skalar dgn GET OID persis. Berkas: `SmartOltSupport::DRIVER_HSAIRPO_EPON` + `hsAirPoEponCapabilities()`, `app/Services/HsAirPo/{HsAirPoSnmp,HsAirPoCliService,HsAirPoEponService}.php` (driver implements `SmartOltSnmpDriver`, di-resolve `SmartOltSnmpServiceResolver`, hasil sesi CLI **di-memo per-OLT** supaya 1 scan = 1 sesi telnet), `HsAirPoOltController` + rute `hsairpo-olt.*` + `Pages/HsAirPo/*`, tab "OLT HsAirPo" (`SmartOltController::index()` kini partisi 4-arah; `Index.vue` memilih data/ikon/prefix rute lewat peta `nonZteFamilies`). Scan penuh & polling terjadwal ikut `CDataOltScanner`/`PollOltJob::pollViaScanner` (faceplate di-skip untuk family ini). CLI telnet gaya Cisco-IOS (`Username:` → `enable` → `EPON-OLT#`, IAC via `TelnetIacFilter`, `terminal length 0`) dgn **timeout keras anti-hang** — firmware ini membekukan sesi pada `show epon port {n} onu all optical-info` (**jangan dipakai**; Rx harus per-ONU). **Rx per-ONU KINI ON** (`supports_cli_rx=true`): `HsAirPoCliService::fetchPortRx` = `show epon port {pon} onu {onu} optical-info` (hanya ONU online, ~2 dtk/ONU). **Dua jalur**: (1) tombol **per-ONU** di kolom Rx (`refreshOnuRx`, rute `hsairpo-olt.onu.rx`, sinkron ~2 dtk); (2) tombol **"Rx semua ONU"** se-port di **background** (`refreshPortRx` → `App\Jobs\RefreshHsAirPoPortRxJob`, rute `hsairpo-olt.port-rx`) — WAJIB background karena sinkron se-port (puluhan ONU × 2 dtk, mis. PON 53 ONU ≈ 122 dtk) menembus 100 dtk Cloudflare → 504; job menulis progres+Rx bertahap, halaman **poll `only:['snapshot','rx_status']`** tiap 5 dtk selama `running`. Rx disimpan di **side-store `last_test_result.hsairpo_rx.{slot}_{port}.{onu}`** (+ `hsairpo_rx_status`) di LUAR `port_onus` supaya **selamat dari scan/poll** (scanner hanya menulis ulang `port_onus`); `HsAirPoOltController::mergeRx` menggabungkannya ke daftar ONU saat render. Refresh port biasa (`refreshPortOnus`) = inventori saja. **Aksi tulis ONU KINI ON** via CLI config `epon port {pon} onu {onu} <verb>` — rename `description <str1-64>`, `reboot`, `delete` (terverifikasi live via context-help; `runConfig` = `configure terminal`→cmd→`end`); bercabang `isHsAirPo` di `HsAirPoOltController` + `OnuMapController` + `Api/V1/OnuActionController`. Masih OFF: **enable/disable** (`supports_onu_toggle=false`; verb `activate`/`no activate` ada tapi semantik belum diuji live) & save-config. ⚠️ Deteksi bergantung field `vendor` memuat needle `hsairpo|hsgq|photon|12170` — **tambahkan OLT lewat tab "OLT HsAirPo"** (bukan tab HiOSO yang default vendor "…25355" → salah jatuh ke HiOSO). Referensi: `docs/SMARTOLT_HSAIRPO_GUIDE.md`.
- **Browser telnet (xterm.js)** — `app/Services/Telnet/TelnetProxyServer.php` is a WebSocket↔telnet proxy (built on react/socket + ratchet/rfc6455 already pulled by Reverb) run via `php artisan telnet:proxy`. `app/Http/Controllers/TelnetSessionController.php` issues a short-lived encrypted ticket (`app/Support/Telnet/TelnetTicket.php`, URL-safe); `TelnetIacFilter` strips/answers IAC. Frontend: `resources/js/Components/Shell/TelnetWindow.vue` (draggable/min/max). In prod the daemon binds localhost and is reached via nginx `/telnet-ws`; runs under supervisor (`kusumavision-telnet-proxy`).
- **TR069 massal (per-port)** — tombol "TR069 Massal" di halaman ONU per port (`Pages/SmartOlt/PortOnus.vue` + `Components/SmartOlt/Tr069BulkModal.vue`) mengaktifkan TR069/ACS di **semua ONU satu PON port** OLT ZTE. `app/Services/ZteTr069BulkService.php` di-scope ke slot/port (lewat `$onlySlot`/`$onlyPort`; null = seluruh OLT, dipakai baris task lama), membaca running-config tiap ONU (1 sesi telnet/port via `ZteOnuRunningConfigService::fetchMany`), **men-skip** ONU yang TR069-nya sudah unlock & ACS-nya (url+username) sudah mengarah ke target, lalu menulis `tr069-mgmt 1 state unlock` + acs line ke sisanya (1 sesi tulis/port). Dua fase lewat flag `execute`: dry-run (pindai saja) lalu eksekusi. Jalan di queued job (`Tr069BulkConfigJob` + tabel `tr069_bulk_tasks` yang menyimpan `slot`/`port`, di-poll). Default ACS dari `config('services.acs')` (`ACS_URL`/`ACS_USERNAME`/`ACS_PASSWORD`) — di-set lewat `.env`/Settings, tidak ada kredensial asli yang di-hardcode di repo. Routes `smartolt.tr069-bulk` (POST `…/ports/{slot}/{port}/tr069-bulk`) + `smartolt.tr069-bulk.status` (GET), gated `supports_onu_config_write` (penulis config gaya C300 — OFF di C600; begitu juga Copy-ONU & register mode Lanjutan).
- `app/Http/Controllers/DashboardSearchController.php` — global search (⌘K): matches OLT by name/ip and ONU by `serial_number`/name/interface from cached `port_onus`.
- **Backup konfigurasi OLT (ZTE)** — `app/Services/Zte/OltConfigBackupService.php` mengambil `show running-config` via `ZteCliProvisioningExecutor`, menyimpan tiap snapshot ke tabel `olt_config_backups` (kolom `content` **encrypted** + `$hidden`, `sha256` untuk **dedup** versi identik beruntun, `trigger` manual/scheduled, `status` ok/failed). Backup terjadwal harian: saklar per-OLT `snmp_olts.config_backup_enabled` → `BackupOltConfigsCommand` (`olts:backup-config`, `Schedule::…->dailyAt('02:30')` di `routes/console.php`) men-dispatch `BackupOltConfigJob` untuk OLT ZTE yang enabled; backup manual berjalan **sinkron** di controller. `OltConfigBackupController` + rute `smartolt.config-backups.*` (index/store/toggle/content/download) — halaman `Pages/SmartOlt/ConfigBackups.vue` (riwayat, toggle harian, backup manual, **diff** dua versi via `resources/js/lib/linediff.js`, lihat/unduh). Otorisasi kepemilikan otomatis via route-model binding + `PartnerOltScope` (admin/operator semua, partner OLT-nya sendiri); backup di-scope ke OLT-nya. Scope v1 ZTE saja (C-Data/HiOSO sintaks CLI beda).
- **Simpan konfigurasi OLT ("Save Config") — semua family, sinkron via CLI** — tombol ikon `Save` di daftar OLT (`Pages/SmartOlt/Index.vue`, tab ZTE **dan** non-ZTE, gated `canManageOlt && cli_transport==='telnet' && capabilities.supports_config_save`; konfirmasi modal + spinner). Menyimpan running-config ke memori OLT (persist, **bukan** backup ke DB — beda dari bullet backup di atas). Command per-family: **ZTE** `write` (`ZteCliProvisioningExecutor::saveConfig`, route `smartolt.config.save`) — write di **C300 config besar bisa hening ~30 detik**, jadi baca pakai `readUntilIdle(quiet=75s, cap=120s)` supaya berhenti hanya saat prompt CLI kembali, bukan patokan output sunyi; **C-Data EPON/GPON** `enable → config → save` (`CDataCliWriteService::saveConfig`, route `cdata-olt.config.save`); **HiOSO** `enable → write` (`HiosoCliWriteService::saveConfig`, route `hioso-olt.config.save`). Semua gated capability `supports_config_save` + `throttle:olt-refresh`. **Beda dari tombol lama "Refresh ONU (scan penuh)"** yang sudah dihapus dari daftar OLT non-ZTE (route `refresh` masih dipakai tombol "Scan ONU" di halaman Detail).
- **Label port PON sisi-NMS (non-ZTE saja)** — hanya ZTE yang bisa menamai port di perangkat (`smartolt.port.description`, capability `supports_port_description_write`, JANGAN diubah). C-Data/HiOSO/HsAirPo tak punya perintah setara yang terverifikasi (probe `ifAlias` live: kosong, atau di C-Data GPON V3 cuma cerminan nama port bawaan agent) → labelnya disimpan di NMS: tabel `olt_port_labels` (unik `snmp_olt_id`+`slot`+`port`, maks 64 char) + `App\Services\OltPortLabelService` (`forOlt()` peta `{slot}_{port}`, `set()` simpan/hapus+sanitasi) + satu rute bersama `POST olts/{olt}/port-label` (`olt.port-label.store`, `OltPortLabelController`, gated `canManageOlt()` + capability **`supports_port_label`** yang sengaja TIDAK ada di ZTE → 403). **Sengaja di luar `last_test_result`** supaya selamat dari scan/poll (pola sama seperti side-store `hsairpo_rx`). UI: komponen bersama `Components/OltPortLabel.vue` — kolom **Label** di tabel port `Pages/{CDataOlt,Hioso,HsAirPo}/Detail.vue` (+ baris di kartu mobile) dan header `Pages/{…}/PortOnus.vue`; prop `port_labels` dikirim dari `detail()`/`portOnus()` ketiga controller. Tak menyentuh telnet/SNMP sama sekali. **Mobile**: `Api/V1/OltController::show` mengisi field `description` per port dari label ini untuk non-ZTE (fallback setelah deskripsi CLI ZTE/C600 `if_descr`) — dipilih memakai field lama supaya tampil di APK terpasang tanpa rilis baru.
- **Peta ONU** — `app/Http/Controllers/OnuMapController.php` + `Pages/Map/Index.vue` (route `map.index`): Leaflet map of customer-ONU pins across all OLTs (ZTE & C-Data). Pins live in table `onu_map_pins` (only a *reference* `snmp_olt_id/slot/port/onu_id` + coords + optional customer fields — ONUs themselves stay cacheless), enriched live via `app/Services/OnuInventoryService.php` (cross-OLT ONU aggregation extracted from `onuMonitor()`, shared by both). Map component `Components/Map/OnuMap.vue` is **lazy-loaded** (`defineAsyncComponent`) and uses **keyless Google tiles** (`mt{s}.google.com/vt`, unofficial/free) + OSM fallback. Add a pin via map-click modal (`AddPinModal.vue`, OLT→port→ONU dropdowns + global search) or the **Add Map** button on Port ONUs pages (paste Google Maps link → `map.resolve-link` parses coords; or "click on map" placement mode). Pin detail (`PinDetailCard.vue`) has edit-name/reboot that hit **map-specific** routes `map.pins.reboot|rename` (delegate to `ZteRemoteOnuService`/`CDataCliWriteService` then redirect back to `/map`, unlike `smartolt.onu.*`/`cdata-olt.onu.*` which redirect to the port page). Pin ONU di peta diwarnai **hijau (online) / merah (offline/LOS/dying-gasp)** saja — bukan lagi level-RX (info RX tetap di kartu detail). **Kinerja (jangan diregresi, perbaikan Jul 2026 5,2 s → 0,09 s):** cast `array` Eloquent men-`json_decode` ULANG tiap akses atribut, jadi `OnuInventoryService` **memo snapshot `last_test_result` per-OLT** (`snapshot()`/`$routePrefixes`) — jangan baca atribut itu langsung di dalam loop (mis. `findOne()` per ONU-ODP); prop berat dibungkus closure (`odps`) dan `onus` = `Inertia::optional()` + dipangkas 12 kolom (`onuOptions()`), hanya dikirim saat frontend minta `only: ['onus']` (masuk mode tambah pin); aksi pin pakai partial reload `only: ['pins'|'odps', 'flash']` — **`flash` wajib ikut** kalau ada toast karena `only` menyaring shared prop juga; `OnuMap.vue` **men-diff marker** (`{ marker, sig }` + `syncMarker()`), bukan `clearLayers()`+bikin ulang (itu yang dulu bikin tiap aksi terlihat seperti reload). See `docs/handbook/16-peta-onu.md`.
- **ODP (Optical Distribution Point)** — konsep splitter lapangan + topologi ODP→ONU. Tabel `odps` (nama + lat/lng + notes, **per-OLT**, `PartnerOltScope`) = pin ODP kuning di peta (bentuk teardrop sama pin ONU, badge jumlah ONU). Relasi ONU↔ODP di `onu_odp_links` ber-key komposit `(snmp_olt_id, slot, port, onu_id)` (ONU tetap tanpa tabel), unik 1 ODP/ONU. `app/Services/OnuOdpService.php` dipakai bersama: `odpsForOlt`/`linksForPort` (prop `odps`+`odp_links` untuk **kolom ODP** di ketiga `Pages/{SmartOlt,CDataOlt,Hioso}/PortOnus.vue` via `Components/OnuOdpCell.vue`), `assign` (kolom dropdown → `onu-odp.assign`), `connectedOnus` (ONU terhubung dienrich online+koordinat pin). `OnuMapController::index` kirim prop `odps`; `OnuMap.vue` menggambar **garis kabel animasi** ODP→ONU (warna ikut status ONU). `OdpController` (`map.odps.store/update/destroy`) + kartu `Components/Map/OdpDetailCard.vue` (edit nama + lihat ONU terhubung + hapus); ODP dibuat lewat toggle jenis ONU/ODP di `AddPinModal.vue`. **Halaman ODP tersendiri** (`odp.index`, nav "ODP", `Pages/Odp/Index.vue`): daftar + filter OLT/port + CRUD + modal **Kelola ONU** (dimuat dari JSON `odp.onus`, tambah/lepas ONU memakai ulang `onu-odp.assign` — tak ada endpoint tulis baru); prefix rute sengaja `odp.*` (bukan `map.odps.index`) supaya penanda menu `map.*` tak ikut menyala, dan `store/update/destroy` memakai `back()` agar bisa dipanggil dari peta maupun halaman ODP. **Filter ODP** ada di Monitoring ONU (kolom + dropdown, data `odp_id`/`odp_name` ditambahkan `OnuInventoryService::normalize()` lewat peta lookup `OnuOdpLink` sekali per request — query model langsung, **jangan** inject `OnuOdpService` ke `OnuInventoryService` karena melingkar; `findOne()` **default tanpa lookup** sebab dipanggil di loop `connectedOnus()`; pemanggil satuan mengaktifkannya lewat `findOne(..., withOdp: true)` — dipakai detail 1 ONU di REST API, jangan di dalam loop) dan di ketiga halaman PortOnus. **Field ODP opsional saat registrasi ONU ZTE** (ketiga form `RegisterOnu.vue`, dropdown disaring per slot/port di klien): dikaitkan **hanya setelah CLI sukses** dan **di luar blok `try`** eksekusi Telnet (kalau lebih awal bisa menimpa kaitan ONU lain di slot/port/onu_id sama; kalau di dalam `try` gagalnya bikin baris audit `failed` kedua) via `OnuOdpService::assignQuietly()` — gagal mengaitkan tak membatalkan registrasi, cuma peringatan `flash.onu_odp_link_failed`. **Foto dokumentasi ODP** (kolom `odps.photo_path`, 1 foto/ODP, unggah baru menimpa): berkas di disk **privat** `local` (`storage/app/private/odp-photos/{odp}/{acak}.webp`), **bukan** `/storage` publik — hanya keluar lewat rute ber-auth `odp.photo` (web) & `api.odps.photo` (aplikasi, token Sanctum), keduanya kena `PartnerOltScope`. Konversi ke WebP oleh **biner `cwebp`** (`App\Services\Odp\OdpPhotoService`, paket apt `webp`, config `services.cwebp`) — **PHP di server ini TIDAK punya GD/Imagick**, jangan tulis kode yang mengandalkan keduanya; kalau `cwebp` absen foto disimpan apa adanya (fitur tetap jalan). `-resize` hanya dipakai bila gambar > `max_dimension` (cwebp juga memperbesar gambar kecil). Batas 12 MB → PHP-FPM butuh `upload_max_filesize ≥ 12M` (`install.sh` menulis `99-kusumavision-uploads.ini`; `check-requirements.sh` memperingatkan). URL ber-`?v=` (hash path) supaya cache klien menyegar saat foto diganti. UI: `Components/Map/OdpPhotoField.vue` (dipakai `OdpDetailCard` di peta + modal `Pages/Odp/Index.vue`); mobile menampilkan lewat `core/widgets/odp_photo.dart` (`Image.network` + header Authorization) **dan bisa unggah/ganti/hapus** dari Detail ODP (ikon kamera → Kamera/Galeri/Hapus, paket `image_picker`, dikecilkan 1600px/q88 di perangkat, **tanpa izin Android baru**) lewat `POST|DELETE /api/v1/odps/{odp}/photo` di grup tulis. Validasi dipakai bersama web & API (`OdpPhotoService::rules()`). Hapus ODP ikut membuang berkasnya. **Warna pin ODP** (kolom `odps.color` nullable, null = amber default): warna dipakai **mengelompokkan ODP per PON port**, jadi aksi simpannya bawaan **menyapu satu port** (`OnuOdpService::setColor()`, `apply_to_port` default true → bulk update `(snmp_olt_id, slot, port)`; ODP tanpa slot/port hanya dirinya). Palet `App\Support\OdpColors::PALETTE` (16 warna, **tanpa hijau/merah** yang dipakai status ONU) = **sumber tunggal** — dikirim ke web sebagai prop `odp_color_palette` (halaman Peta & ODP) dan ke aplikasi lewat `meta.color_palette` di `GET /api/v1/odps`; klien hanya menyimpan default + hitungan kontras (`resources/js/lib/odpColors.js`, `mobile/lib/core/odp_colors.dart`). "Acak" dihitung **server** (`OdpColors::randomFor()` menghindari warna yang sudah dipakai port lain di OLT itu) supaya web & mobile identik. Rute `POST map.odps.color` (web, `back()`) + `POST /api/v1/odps/{odp}/color` (grup tulis `role:admin,operator,partner` + `BlockDemoWrites`; **satu-satunya aksi tulis ODP dari aplikasi**). UI: `Components/Map/OdpColorModal.vue` (dipakai `OdpDetailCard` di peta & `Pages/Odp/Index.vue`, submit `only: ['odps','flash']`), mobile `features/odp/odp_color_sheet.dart` (palet + acak + saklar se-port, tanpa hex bebas). ⚠️ `sig` marker di `OnuMap.vue::renderOdps` **harus memuat warna** — kalau tidak, diff menganggap marker tak berubah dan pin tetap warna lama. Garis kabel ODP→ONU **tetap** hijau/merah status (bukan warna ODP). **Lock/unlock posisi pin** (kolom `locked` default true di `onu_map_pins` **dan** `odps`): tombol di `PinDetailCard`/`OdpDetailCard` → `PUT map.pins.update`/`map.odps.update` `{locked}`; saat terbuka marker Leaflet `draggable` dan `dragend` **langsung menyimpan** koordinat (PUT lat/lng, tanpa flash supaya tak berisik) — tombol Kunci hanya menutup. **Aplikasi Android**: ODP & peta sudah ada (tab ODP + tab Peta, baca-saja) lewat endpoint `GET /api/v1/{odps,odps/{odp}/onus,map}`; `connectedOnus()` kini juga mengirim `rx_power_dbm`/`rx_power_label` (dipakai kartu ODP web + daftar ONU dalam ODP di aplikasi). CRUD ODP & pin tetap web-only.
- **Navigasi kontekstual bel notifikasi** — klik notifikasi alarm langsung membuka ONU/port/OLT-nya. Tujuan diputuskan **server**, bukan browser: `POST notifications.alarms.open` → `app/Services/Alarm/AlarmNotificationTargetResolver.php`. Sumber lokasi HANYA kolom terstruktur `alarm_events` (`scope/slot/port/onu_id/serial_number`) — **jangan pernah** parse `message` (teks terlokalisasi). Resolver cuma baca cache `last_test_result` (**tanpa SNMP/Telnet**, ini jalur klik user): cari `serial_number` dulu → kalau ONU pindah port, buka posisi **sekarang** (`onu_moved`); kalau serial tak ketemu tapi posisi historis kini ditempati serial LAIN → **tolak** (`position_reused`) agar tak membuka ONU pelanggan yang salah; ONU hilang → `onu_not_found` + fallback `alarms.index` ter-filter. `onu` → `smartolt.onu.detail` bila capability `supports_cli_onu_detail` true (pakai **capability**, bukan nama family), selain itu `{prefix}.port-onus?focus={onuId}` (prop `focus` sudah ada di ketiga halaman PortOnus — **tak perlu menyentuh modul C-Data/HiOSO**). Payload Inertia (`HandleInertiaRequests` → `AlarmNotificationService`) mengirim ID terstruktur + `is_read` tapi **sengaja tanpa `target_url`** (menghitungnya = decode snapshot ~1400 ONU × 8 alarm di SETIAP request). Baca per-notifikasi di tabel `alarm_notification_reads` (pivot user↔alarm); `users.last_notifications_read_at` tetap dipakai untuk "tandai semua"; `unread_count` = `COUNT` semua alarm aktif belum dibaca (bukan cuma 8 yang tampil). **API/mobile pakai resolver yang sama** lewat `resolveLocation()` → blok `target` di `GET /api/v1/alarms` (`resource_type` + slot/port/onu_id **sekarang** + `openable` + `reason`); `openable` di situ TIDAK memakai `supports_cli_onu_detail` (app punya layar detail ONU sendiri via API, jalan untuk semua family). Field `slot/port/onu_id` tingkat-atas di API/FCM adalah posisi **historis** — **jangan** dipakai untuk navigasi (`mobile/.../alarm_list_screen.dart` pernah begitu dan bisa membuka ONU pelanggan yang salah); pakai `target`. Indeks serial→posisi dimemoisasi per-OLT di resolver karena daftar alarm bisa 200 baris dari OLT yang sama.
- Frontend: `resources/js/Pages/SmartOlt/*.vue` (Inertia pages).
- **Aplikasi Android (Flutter)** — `mobile/` (Flutter 3.44, Riverpod v2, dio, go_router, Material 3 dark-glass cyan). Toolchain build ada di server (`/opt/{flutter,android-sdk}`, JDK17, env `/etc/profile.d/flutter.sh`); build via `bash bin/build-apk.sh` (`API_BASE_URL=...`) → `mobile/build/.../app-release.apk`, disalin ke `public/downloads/kusumavision-nms.apk` (gitignored). **`mobile/android/gradle.properties` dikonstrain untuk server 8GB** (heap 2g, daemon off, worker 2) — default `-Xmx8G` pernah swap-thrash sampai reboot; jangan build sebagai www-data. `namespace` = `net.kusumavision.kusumavision_nms` (harus cocok package `MainActivity.kt`), `applicationId` = `net.kusumavision.nms` (= `google-services.json`). **Navigasi bawah 5 tab: Dashboard · OLT · ODP · Peta · Akun** (Jul 2026) — Alarm & Pencarian bukan tab lagi: alarm dibuka dari kartu di Akun/Dashboard, pencarian dari ikon 🔍 di AppBar Dashboard (menggantikan tombol logout, logout kini hanya di Akun); keduanya rute root yang di-`push` (deep-link FCM alarm `go('/dashboard')` lalu `push('/alarms')` supaya Back tak keluar aplikasi). Fitur: login, dashboard, pencarian, OLT→port→ONU, unconfigured+registrasi (ZTE mode dasar, **ODP opsional** lewat dropdown tersaring per slot/port), reboot/rename, alarm, **ODP** (daftar+cari+filter OLT, detail berisi ONU di dalamnya + cari), **Peta** ONU/ODP satu layar penuh (`flutter_map`+`latlong2`, tanpa API key; tile Google keyless + `fallbackUrl` OSM + UA browser; navbar tetap terlihat; baca-saja — pin & CRUD ODP tetap di web; fokus lintas-layar lewat `mapFocusProvider`, BUKAN query URL karena tab hidup di IndexedStack), **Akun** (info+logout+tes push+pintu masuk alarm), push FCM. Aktivasi FCM: `mobile/android/app/google-services.json` + `storage/app/firebase/service-account.json` + `FIREBASE_CREDENTIALS` di `.env`. Konsumsi **REST API v1** (di bawah). **Design system (pasca-rombak total Jul 2026, tetap dark-glass cyan):** font **bundle** Sora/Inter/JetBrainsMono di `mobile/assets/fonts/` (offline-first, variable — `fontWeight`→axis `wght`; token `AppFont`/`AppText.mono` di `theme/app_theme.dart` bersama `AppMotion`/`AppGradient`); latar `core/widgets/aurora_background.dart` (aurora mesh + node-fiber, CustomPainter hand-rolled, flag `animate` off untuk daftar panjang); widget bersama `signal_ring`/`pulse_dot`/`pulse_logo`/`count_up_text`/`stagger`/`GlassCard` v2 (opsi `blur`); deps `flutter_animate` + `flutter_staggered_animations` + `shimmer`. **Wajib bump `version:` (versionCode) tiap rilis APK** — versionCode identik ditolak Android saat update. Rencana & prinsip lengkap: `mobile/DESIGN_REVAMP_PLAN.md`.
- **REST API v1 (aktif)** — `routes/api.php` (`$apiEnabled=true`), controllers `app/Http/Controllers/Api/V1/*`, Sanctum bearer token, throttle 120/mnt. Baca (summary, olts+capabilities, onus, port-onus, search via `app/Services/GlobalSearchService.php`, unconfigured, alarms, register/options, **odps + odps/{odp}/onus + map** via `Api/V1/{OdpController,MapController}`; payload peta dirakit `app/Services/Map/OnuMapPayloadService.php` yang **dipakai bersama halaman web** `OnuMapController` — ubah field pin di situ saja) + tulis grup `role:admin,operator,partner`+`BlockDemoWrites` (register preview/store via `app/Services/Zte/OnuRegistrationService.php` & refresh = ZTE-only; **reboot/rename/delete bercabang per-family** ZTE/C-Data/HiOSO di `OnuActionController` — delete via `DELETE olts/{olt}/onus/{slot}/{port}/{onuId}`, gated `supports_onu_delete`; ZTE delete reuse `ZteRemoteOnuService::delete`). **NB rute API ikut di-cache** (`routes-v7.php`) — rute baru 404/405 (termasuk di test) sampai `php artisan route:cache` ulang. Docs `docs/API.md`.
- **Push FCM (mobile)** — `app/Services/Fcm/FcmAlarmNotifier.php` (kreait/laravel-firebase) di-hook di `AlarmEvaluator` di samping Telegram, via queued `app/Jobs/SendFcmAlarmNotifications.php`. Token perangkat `fcm_device_tokens` (`Api/V1/DeviceController`, register saat login mobile). `fcm_settings` (`FcmSetting`, singleton) kini hanya **saklar kanal** — filter alarmnya terpusat di `alarm_settings` (lihat bullet "Pengaturan alarm TERPUSAT"); tab **"Notifikasi Mobile"** mengurus saklar push, **kirim notif manual** ke semua perangkat (`SettingsController::updateFcm`/`sendFcmManual`), dan daftar perangkat. Dormant/no-op tanpa kredensial (polling aman). `notifier->active()` = kredensial ada + saklar Settings on.
- **Saklar alarm per-OLT (per-PENERIMA, bukan mute evaluasi)** — tombol On/Off per OLT di `Pages/SmartOlt/Index.vue` (route `smartolt.alarms.toggle`, satu route utk semua family). `AlarmEvaluator::evaluate()` **selalu** jalan (event tetap tercatat); yang di-gerbang hanya pengiriman notifikasi per-penerima: kolom `snmp_olts.alarms_enabled` = saklar **admin+operator** (operator ngikut admin) → gerbang bot global Telegram (`TelegramNotifier::configsFor`) + FCM admin/operator (`FcmAlarmNotifier::recipientUserIds`); pivot `olt_user.alarms_enabled` = saklar **per-partner-per-OLT** → gerbang bot Telegram partner + FCM partner (independen dari flag admin). `SmartOltController::toggleAlarms` bercabang role; `serializeOlt.alarms_enabled` viewer-effective (partner lihat pivot-nya). **NB:** `test()` (Test SNMP) harus `array_merge` ke `last_test_result`, bukan menimpa — kalau menimpa, ports/ONU hasil poll terhapus jadi 0.
- **Debounce konfirmasi 2 poll (anti-flap, semua jenis alarm & semua OLT)** — `AlarmEvaluator` tak lagi mengirim notifikasi pada deteksi pertama. Fault baru dicatat status **`AlarmEvent::STATUS_PENDING`** (belum dikirim, TAK tampil di UI/hitungan aktif — `AlarmController` web+API filter `whereIn active,cleared`; bell/dashboard/Telegram-count pakai `STATUS_ACTIVE`). Notifikasi raise baru dikirim bila fault **masih ada di poll berikutnya** (promote PENDING→ACTIVE); bila pulih sebelum konfirmasi, baris pending **dihapus diam-diam** (tak ada notif down maupun clear). `openAlarms()` ambil ACTIVE+PENDING; deteksi transisi (port/onu/rx/unreachable) pakai keduanya. Interval poll umum 5 mnt → alarm sungguhan datang ~2 poll (~10 mnt).
- **Korelasi root-cause (anti alarm-storm)** — `AlarmEvaluator` menahan banjir alarm anak, saklar `alarm_settings.suppress_child_alarms` (Settings → Alarm, default ON): (1) OLT unreachable (`snapshot.ok=false`) → port & ONU tak dievaluasi sama sekali; (2) sebuah PON **port down** mensupres alarm ONU-offline/LOS/dying-gasp **baru** untuk ONU di port itu (himpunan `$downPorts` "slot/port"), pesan port-down menyebut jumlah ONU terdampak (`meta.affected_onus`); (3) **SEMUA ONU satu ODP offline** (≥2 ONU, port induknya masih up) → satu alarm **`odp_down`** (scope `odp`, severity major, signature `odp:{id}:odp_down`, `meta.odp_id/odp_name/affected_onus`), alarm ONU anggotanya disupres — status per-ODP dihitung `OdpAlarmGrouper::statuses()` dari snapshot (`total` = ONU ODP yang muncul di snapshot, `down` = yang offline). Episode ONU yang **sudah terbuka** saat induknya turun tetap direkonsiliasi (dijaga `onuHasStateAlarm`) supaya tak ter-clear palsu, tapi ditandai **`meta.notified=false`** → notifikasi raise **dan** clear-nya dilewati (kalau tidak, pemulihan port/ODP = banjir pesan "cleared"); begitu induknya pulih & ONU masih mati, tanda dilepas dan alarm dikirim sebagai gangguan mandiri (`$parentRecovered` juga membuka ONU yang tak punya transisi online→offline karena matinya tertutup gangguan induk). ODP yang gangguannya terlanjur tercatat per-ONU diangkat sekali jadi satu `odp_down` (`$hasOpenChildren`) agar pemulihannya punya induk yang melapor. ODP yang **sebagian** ONU-nya down tetap dirangkum di layer notifikasi jadi 1 pesan (`OdpAlarmGrouper::group()`, saklar `group_odp_alarms`; grup juga berlaku untuk pesan pemulihan). Interpretasi kode `last_down_cause` ke bahasa Indonesia di UI: `resources/js/lib/onu.js` (`lastDownCauseLabel`).
- **Pengaturan alarm TERPUSAT (Settings → tab Alarm)** — `alarm_settings` (model `AlarmSetting`) satu-satunya sumber kebijakan alarm: `confirm_before_notify` (debounce 2 poll vs realtime), `min_severity`, `notify_on_raise`, `notify_on_clear`, `notify_types` (null = semua jenis), `suppress_child_alarms`, `group_odp_alarms`. `TelegramSetting` (bot global) & `FcmSetting` **mendelegasikan** `minSeverityRank()`/`notifyTypes()`/`shouldNotifyType()`/`notifyOnRaise()`/`notifyOnClear()` ke situ; kolom senama di kedua tabel kanal dipertahankan tapi TAK dipakai lagi (rollback-safe), dan endpoint `settings.telegram.update`/`settings.fcm.update` tak lagi menerima field filter — tab kanal cuma koneksi (token/chat id/webhook, saklar push, daftar perangkat). **Bot partner (`PartnerTelegramBot`) tetap punya filter sendiri** (diatur partner di halamannya).

## Conventions

- **Test SELALU lewat `bash scripts/test.sh`** (alias `composer test`), **jangan pernah** `php artisan test` / `phpunit` polos. Checkout ini melayani produksi, dan `bootstrap/cache/config.php` dimuat lebih dulu saat boot lalu **menang** atas `<env>` di `phpunit.xml` — koneksi resolve ke pgsql `kusumavision_nms` walau `DB_CONNECTION=sqlite` sudah diset (terukur 10 Agu 2026). 53 file test memakai `RefreshDatabase` → `migrate:fresh` pada koneksi default = **drop seluruh tabel produksi**. Skrip itu mengalihkan `APP_CONFIG_CACHE`+`APP_ROUTES_CACHE` ke path non-eksisten (hanya dua ini — `APP_SERVICES_CACHE`/`APP_PACKAGES_CACHE` ditulis ulang saat boot, path tak-writable = exception) lalu **abort** kecuali probe menghasilkan `sqlite|:memory:`. `config:clear` **bukan** solusi: DB aman tapi cache config produksi terhapus permanen. Baseline hijau: 493 passed / 2606 assertions, `npm test` 12 passed.
- Provisioning writes an audit row in `smartolt_onu_registrations` (script generated first, executed later/optionally). Profiles are scoped per-OLT with global (`snmp_olt_id = null`) fallback.
- `SMARTOLT_ZTE_C300_C320_C600_GUIDE.md` is the **authoritative ZTE CLI command reference** — consult it over guessing command syntax. Untuk **C600** pakai `docs/SMARTOLT_ZTE_C600_GUIDE.md` (guide lama bagian C600 pernah salah total). **OID vendor hanya boleh masuk kode setelah dibaca dari perangkat asli** — kalau tak bisa diverifikasi, biarkan `null` dan matikan capability-nya. PDF C600 di `docs/*.pdf` **tak terverifikasi**, jangan dipakai sebagai sumber OID.
- `WORKLOG.md` logs work phase-by-phase (Created/Changed/Notes, with real-OLT verification). Add a matching entry for meaningful changes.
- **Seluruh string user-facing dwibahasa ID/EN** (default `id`): frontend via vue-i18n `$t('ns.key')` (`resources/js/lang/{id,en}.json`, ~36 namespace; custom messageCompiler = literal + `{param}` saja, TANPA linked/plural), backend via `__()` (`lang/{id,en}/` — `flash.php` untuk flash controller, `reports.php` ikut CSV/PDF, `validation/auth` ID). Fitur baru WAJIB menambah key ke **kedua** bahasa; opsi ber-enum stabil diterjemahkan frontend by-key (`lib/alarm.js`); switch bahasa: `LanguageSwitcher` + middleware `SetLocale` + kolom `users.locale`.
- **UI/tema dashboard** (dark glass, aksen cyan/sky, kelas `kv-*` di `resources/css/app.css`, shell `AuthenticatedLayout`): saat menambah/ubah halaman atau komponen, ikuti aturan di `docs/handbook/15-ui-tema-dashboard.md` (pakai `kv-*` dulu, kartu kaca semi-transparan, tabel responsif desktop+mobile, ikon Lucide, gerbang `auth.can`, string Indonesia).
- Live test OLTs: id=1 (`OLT-C320-PATI`) and id=2.
- **Prod runs cached config** (`bootstrap/cache/config.php`); `.env` is `640 root:www-data` so the app user can read it. After any `.env`/config change run `php artisan config:cache` so the cache picks it up, then restart the supervisor daemons (`queue:restart` for `kusumavision-worker`, `supervisorctl restart kusumavision-telnet-proxy`). Long-lived daemons need a restart to pick up code changes. (Keep `.env` group-readable by `www-data` — if it's `root:root`, a cleared config falls back to sqlite and the site 500s.)
- **Fresh-server deploy** is automated by `install.sh` (installs runtime, DB, `.env`, build, Go poller, migrations, nginx site + supervisor daemons `kusumavision-worker`/`-scheduler`/`-telnet-proxy`). `scripts/check-requirements.sh` validates tools/extensions/services. Full walkthrough: `docs/handbook/04-instalasi-deploy.md`.
- **Docker "appliance" deploy** (install lengkap di 1 PC seperti NetNumen, buat dibagikan ke banyak lokasi) — `Dockerfile` (multi-stage: node build → `public/build`, go build → `bin/kv-snmp-poller`, php:8.3-fpm + nginx + supervisor) + `docker-compose.yml` (service `app` all-in-one, `db` postgres:16, `redis` redis:7; volumes `pgdata`/`redisdata`/`app_storage`). Konfig di `docker/` (`nginx.conf`, `supervisord.conf` = daemon sama seperti install.sh, `entrypoint.sh`, `php.ini`). `entrypoint.sh` menyiapkan storage skeleton (volume mask), **auto-generate & persist `APP_KEY`** di volume bila kosong, `migrate --force`, admin opsional (`ADMIN_*`), `optimize`, lalu exec supervisord. Env runtime dari compose (bukan file `.env` Laravel); user tune via `.env` host-side (`.env.docker.example`). Launcher 1-klik `start.bat`/`stop.bat`/`update.bat`/`start.sh`. **Tak ada perubahan kode app** — paritas fungsional dengan `install.sh`. Panduan operator `docs/DOCKER.md`, arsitektur `docs/handbook/18-docker-appliance.md`.
